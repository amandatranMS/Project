{
  "name": "msx-milestone-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Synthetic MSX-style mock application for Solution Engineering opportunity and milestone management. Not connected to real MSX, Dataverse, or any production Microsoft system.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "^20.0.0 || >=22.0.0"
  },
  "scripts": {
    "dev": "concurrently --raw -n api,web -c blue,green \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "tsx watch apps/api/src/server.ts",
    "dev:web": "npm run dev -w @msx/web",
    "build": "npm run build -w @msx/shared && npm run build -w @msx/api && npm run build -w @msx/web",
    "test": "npm run test -w @msx/api",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "db:push": "prisma db push",
    "seed": "tsx prisma/seed.ts",
    "db:seed": "tsx prisma/seed.ts",
    "import-workbook": "tsx scripts/parseWorkbook.ts",
    "openapi:json": "node scripts/openapiToJson.mjs",
    "reset-db": "prisma migrate reset",
    "db:reset": "prisma db push --force-reset && npm run db:seed",
    "setup": "npm install && npm run prisma:generate && npm run db:push && npm run import-workbook"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prisma": "^6.2.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "yaml": "^2.9.0"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "xlsx": "^0.18.5"
  }
}
